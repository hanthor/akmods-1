---
#
# WARNING THIS IS A GENERATED WORKFLOW. DO NOT EDIT BY HAND!
#
# Generate the workflow by running `just generate-workflows` at git root
# Modify the inputs in workflow-templates
name: Build ALMALINUX akmods
on:
  merge_group:
  pull_request:
    branches:
      - main
    paths-ignore:
      - '**.md'
  schedule:
    - cron: '10 0 * * *'  # 0010 UTC everyday
  workflow_dispatch:
jobs:
  cache_kernel_almalinux_10-kitten:
    name: Cache almalinux (10-kitten)
    uses: ./.github/workflows/reusable-cache-kernel.yml
    secrets: inherit
    permissions:
      actions: read
      contents: read
      packages: write
    with:
      architecture: '["aarch64","x86_64","x86_64-v2"]'
      kernel_flavor: almalinux
      version: 10-kitten
  build-almalinux_10-kitten_common:
    name: Build common almalinux (10-kitten)
    uses: ./.github/workflows/reusable-build.yml
    secrets: inherit
    permissions:
      actions: read
      contents: read
      packages: write
    needs: cache_kernel_almalinux_10-kitten
    with:
      akmods_target: common
      architecture: '["aarch64","x86_64","x86_64-v2"]'
      kernel_cache_key: ${{ needs.cache_kernel_almalinux_10-kitten.outputs.KCKEY }}
      kernel_flavor: almalinux
      version: 10-kitten
  build-almalinux_10-kitten_nvidia:
    name: Build nvidia almalinux (10-kitten)
    uses: ./.github/workflows/reusable-build.yml
    secrets: inherit
    permissions:
      actions: read
      contents: read
      packages: write
    needs: cache_kernel_almalinux_10-kitten
    with:
      akmods_target: nvidia
      architecture: '["aarch64","x86_64","x86_64-v2"]'
      kernel_cache_key: ${{ needs.cache_kernel_almalinux_10-kitten.outputs.KCKEY }}
      kernel_flavor: almalinux
      version: 10-kitten
  check-almalinux_10-kitten:
    name: Check almalinux (10-kitten)
    permissions:
      actions: read
      contents: read
    needs: [build-almalinux_10-kitten_common,build-almalinux_10-kitten_nvidia]
    runs-on: ubuntu-24.04
    if: always()
    steps:
      - name: Check for Build Failures
        if: contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') || contains(needs.*.result, 'skipped')
        run: exit 1

      - name: Exit if Success
        run: exit 0
  cache_kernel_almalinux_10:
    name: Cache almalinux (10)
    uses: ./.github/workflows/reusable-cache-kernel.yml
    secrets: inherit
    permissions:
      actions: read
      contents: read
      packages: write
    with:
      architecture: '["aarch64","x86_64","x86_64-v2"]'
      kernel_flavor: almalinux
      version: 10
  build-almalinux_10_common:
    name: Build common almalinux (10)
    uses: ./.github/workflows/reusable-build.yml
    secrets: inherit
    permissions:
      actions: read
      contents: read
      packages: write
    needs: cache_kernel_almalinux_10
    with:
      akmods_target: common
      architecture: '["aarch64","x86_64","x86_64-v2"]'
      kernel_cache_key: ${{ needs.cache_kernel_almalinux_10.outputs.KCKEY }}
      kernel_flavor: almalinux
      version: 10
  build-almalinux_10_nvidia:
    name: Build nvidia almalinux (10)
    uses: ./.github/workflows/reusable-build.yml
    secrets: inherit
    permissions:
      actions: read
      contents: read
      packages: write
    needs: cache_kernel_almalinux_10
    with:
      akmods_target: nvidia
      architecture: '["aarch64","x86_64","x86_64-v2"]'
      kernel_cache_key: ${{ needs.cache_kernel_almalinux_10.outputs.KCKEY }}
      kernel_flavor: almalinux
      version: 10
  check-almalinux_10:
    name: Check almalinux (10)
    permissions:
      actions: read
      contents: read
    needs: [build-almalinux_10_common,build-almalinux_10_nvidia]
    runs-on: ubuntu-24.04
    if: always()
    steps:
      - name: Check for Build Failures
        if: contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') || contains(needs.*.result, 'skipped')
        run: exit 1

      - name: Exit if Success
        run: exit 0
